# Copy to .env for local `npm run dev`. In Docker Compose, the equivalent
# value comes from the root .env's TURNSTILE_SITE_KEY (passed as a build arg).
# Leave blank to disable the login captcha widget locally.
VITE_TURNSTILE_SITE_KEY=
